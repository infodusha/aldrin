{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npm run build -w aldrin",
    "watch": "npm run watch -w aldrin",
    "start:example": "run-s build start:example:run",
    "start:example:run": "npm run start -w example",
    "watch:example": "run-p build watch:example:run",
    "watch:example:run": "npm run watch -w example"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
